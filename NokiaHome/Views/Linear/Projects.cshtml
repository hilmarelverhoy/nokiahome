@model List<NokiaHome.Models.Linear.LinearProject>
@{
    ViewData["Title"] = "Projects";
}

<div class="mb-3" style="overflow:hidden">
    <div style="float:right;display:inline-flex;gap:.4rem">
        <a asp-action="CreateProject" class="btn btn-sm btn-dark">+ New project</a>
        <a asp-action="Index" class="btn btn-sm btn-outline-secondary">&larr; Back to issues</a>
    </div>
    <h2 class="mb-0">Projects</h2>
</div>

@if (ViewBag.ErrorMessage != null)
{
    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
}

@if (!Model.Any())
{
    <p class="text-muted">No projects found.</p>
}
else
{
    @foreach (var project in Model)
    {
        var dotColor = string.IsNullOrEmpty(project.Color) ? "#6c757d" : project.Color;
        <a asp-action="ProjectDetail" asp-route-id="@project.Id"
           class="d-block text-decoration-none p-3 rounded border bg-white text-dark mb-2">
            <div>
                <span style="display:inline-block;width:10px;height:10px;border-radius:50%;background:@dotColor;vertical-align:middle;margin-right:6px"></span>
                <span class="fw-semibold" style="vertical-align:middle">
                    @if (!string.IsNullOrEmpty(project.Icon))
                    {
                        <span>@project.Icon </span>
                    }@project.Name
                </span>
                @if (project.State != null)
                {
                    <span class="badge bg-@project.StateBadgeClass" style="margin-left:6px;vertical-align:middle">@project.StateLabel</span>
                }
                @if (project.Progress.HasValue)
                {
                    <span class="text-muted small" style="margin-left:6px;vertical-align:middle">@((int)(project.Progress.Value * 100))%</span>
                }
            </div>
            @if (!string.IsNullOrEmpty(project.Description))
            {
                <div class="text-muted small mt-1">@project.Description</div>
            }
            @if (project.Progress.HasValue && project.Progress.Value > 0)
            {
                <div class="progress mt-2" style="height:4px">
                    <div class="progress-bar bg-dark" role="progressbar"
                         style="width:@((int)(project.Progress.Value * 100))%"
                         aria-valuenow="@((int)(project.Progress.Value * 100))"
                         aria-valuemin="0" aria-valuemax="100"></div>
                </div>
            }
        </a>
    }
}
